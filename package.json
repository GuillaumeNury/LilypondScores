{
  "name": "lilypond-scores",
  "version": "1.0.0",
  "main": "lib/build.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "node lib/build.js",
    "release": "node lib/release.js && node lib/generateHtml",
    "start": "nodemon --config lib/nodemon.json"
  },
  "devDependencies": {
    "nodemon": "^1.18.4"
  }
}
